

workdir: workflow.current_basedir

configfile: "defaults/config.yaml"

segment = ["m", "l", "s"]

# Species names should contain no spaces and instead "_"
species= [
    "Orthohantavirus_andesense", "Orthohantavirus_tulaense", "Orthohantavirus_hantanense",
    "Orthohantavirus_seoulense", "Orthohantavirus_puumalaense"
]

include: "rules/prepare_sequences.smk"
include: "rules/construct_phylogeny.smk"
include: "rules/annotate_phylogeny.smk"
include: "rules/export.smk"

rule all:
    input:
        # expand("results/{species}/{segment}/length_graph.png", segment=segment, species=species),
        expand("auspice/{species}_{segment}.json", segment=segment, species=species),


